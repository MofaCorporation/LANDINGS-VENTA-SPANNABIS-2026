# Expone la misma carpeta public/ también bajo /drops (URLs tipo /drops/es/lady-cupcake).
Alias /drops /var/www/html/public

<Directory /var/www/html/public>
    Options -Indexes +FollowSymLinks
    AllowOverride All
    Require all granted
</Directory>
